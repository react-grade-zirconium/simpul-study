# Subject pages

포털 iframe으로 불러오는 과목별 학습 HTML을 모아두는 폴더입니다.

- 새 과목/단원 페이지를 추가하면 `portal.html`의 메뉴 `data-src`와 `scripts/portal.js`의 `AI_SUBJECTS` 경로도 함께 업데이트하세요.
- 스마트 AI는 이 폴더의 HTML 텍스트를 읽어 과목별 연습문제를 생성합니다.
